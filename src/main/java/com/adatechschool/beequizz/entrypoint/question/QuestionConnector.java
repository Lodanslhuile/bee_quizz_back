package com.adatechschool.beequizz.entrypoint.question;

public interface QuestionConnector {

    // va être appele par le Rest Controller (couche HTTP)
    //  Il va être implémenter par la couche métier qui aujourd'hui s'appelle QuestionService dans core
}
